# Backend API base (used by services/api.ts)
VITE_API_URL=https://avagama-backend-ckm9.onrender.com

# Public frontend URL used to build shareable agent links (Channels tab).
# Switch this to your EC2 host later without any code change, e.g.:
#   VITE_PUBLIC_APP_URL=https://your-ec2-host
# VITE_PUBLIC_APP_URL=https://avagamaai.netlify.app
VITE_PUBLIC_APP_URL=http://localhost:3000

# External URL for the Sales Order Agent embedded in Agent Solutions.
# Swap to a public, embed-friendly production URL later (no code change needed).
# VITE_SALES_ORDER_AGENT_URL=https://sales-order-agent-3v8hz9lhe-avaali.vercel.app/

# External URL for the Material Identification Agent (Render — embeds inline).
# VITE_MATERIAL_AGENT_URL=https://material-agent.onrender.com/
